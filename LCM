#include <stdio.h>
void main()
{
    int a,b,big,step;
    printf("Enter two number:");
    scanf("%d %d",&a,&b);
    big=a>b?a:b;
    step=big;
    while(1)
    {
      if(big %a==0 && big % b==0)
        break;
        else
        big=big+step;
    }
    printf("lcm is %d",big);
}
